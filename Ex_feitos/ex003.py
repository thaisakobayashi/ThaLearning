"""Crie um script Python que leia dois números e tente mostrar a soma entre eles."""

# primeiro = input('Digite um número: ')
# segundo = input('Digite outro número: ')
# soma = primeiro + segundo
# print('O primeiro número escolhido foi ' + primeiro,
# 'e o segundo número foi ' + segundo, 'a soma entre eles é: ' + soma + '.') 

primeiro = float(input('Digite um número: '))
segundo = float(input('Digite mais um número: '))
soma = primeiro + segundo
print('A soma entre {} e {} é igual a: {}'.format(primeiro, segundo, soma))

# primeiro = int(input('Digite um número)) - int = números inteiros
